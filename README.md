# Вопросы для собеседования **backend Rust-разработчика**, с разбивкой по темам: **SQL**, **Postgres**, **Docker**, **Git**, **Linux**, **Web**.

---

## 📦 **SQL**

**Базовые:**

1. <details><summary>Что такое `JOIN`? Разница между `INNER JOIN`, `LEFT JOIN`, `RIGHT JOIN` и `FULL JOIN`.</summary>

    `JOIN` — это операция в SQL, позволяющая объединять строки из двух или более таблиц по связанным колонкам (обычно по первичному и внешнему ключу). Она используется, когда данные хранятся в разных таблицах, но их нужно вывести в одном наборе результатов.

    **Виды JOIN:**

    1. **`INNER JOIN`** — возвращает только те строки, для которых найдено совпадение в обеих таблицах.
    *Пример:* пользователи, у которых есть заказы.

    2. **`LEFT JOIN`** (или `LEFT OUTER JOIN`) — возвращает все строки из левой таблицы и совпавшие строки из правой. Если совпадений нет, в правых колонках будут `NULL`.
    *Пример:* все пользователи, даже без заказов.

    3. **`RIGHT JOIN`** (или `RIGHT OUTER JOIN`) — противоположность `LEFT JOIN`: возвращает все строки из правой таблицы и совпавшие строки из левой. Если совпадений нет, в левых колонках будут `NULL`.
    *Пример:* все заказы, даже без зарегистрированного пользователя.

    4. **`FULL JOIN`** (или `FULL OUTER JOIN`) — возвращает все строки из обеих таблиц, совпадающие и несовпадающие. Несовпавшие поля заполняются `NULL`.
    *Пример:* полный список пользователей и заказов, даже если они не связаны.

    📌 Кратко:

    * `INNER` — пересечение.
    * `LEFT` — всё из левой + пересечение.
    * `RIGHT` — всё из правой + пересечение.
    * `FULL` — объединение всех строк.

    ---

</details>

2. <details><summary>Как работает `GROUP BY` и зачем нужен `HAVING`?</summary>

    `GROUP BY` в SQL используется для **группировки строк с одинаковыми значениями** в указанных столбцах, чтобы к этим группам можно было применять агрегатные функции (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX` и т.д.).

    Например, если у нас есть таблица заказов, мы можем сгруппировать их по ID клиента и посчитать, сколько заказов сделал каждый:

    ```sql
    SELECT customer_id, COUNT(*) AS total_orders
    FROM orders
    GROUP BY customer_id;
    ```

    Здесь каждая группа строк с одинаковым `customer_id` превращается в одну строку с результатом агрегатной функции.

    ---

    ### Зачем нужен `HAVING`

    `HAVING` фильтрует **уже сгруппированные данные**, в отличие от `WHERE`, который фильтрует строки **до** группировки.
    Например, чтобы выбрать только тех клиентов, у которых больше 5 заказов:

    ```sql
    SELECT customer_id, COUNT(*) AS total_orders
    FROM orders
    GROUP BY customer_id
    HAVING COUNT(*) > 5;
    ```

    * `WHERE` нельзя использовать для фильтрации по результату агрегатной функции, потому что она вычисляется **после** группировки.
    * `HAVING` как раз предназначен для таких случаев.

    ---

    📌 **Кратко:**

    * `GROUP BY` — группирует строки по значениям столбцов.
    * `HAVING` — фильтрует группы после применения агрегатных функций.

    ---

</details>

3. <details><summary>Что делает `DISTINCT` и чем он отличается от `GROUP BY`?</summary>

    `DISTINCT` в SQL удаляет дубликаты из результата запроса, возвращая только уникальные строки (или уникальные комбинации значений указанных столбцов).

    Например:

    ```sql
    SELECT DISTINCT country
    FROM customers;
    ```

    Вернёт список стран без повторений.

    ---

    ### Разница с `GROUP BY`

    Хотя оба могут давать похожий результат (уникальные значения), они имеют разные цели:

    | `DISTINCT`                                       | `GROUP BY`                                                             |
    | ------------------------------------------------ | ---------------------------------------------------------------------- |
    | Убирает дубликаты, просто фильтруя результат.    | Группирует строки для последующей агрегации.                           |
    | Нельзя напрямую применять агрегатные функции.    | Обычно используется вместе с агрегатами (`COUNT`, `SUM`, и т.д.).      |
    | Короче и проще, когда нужна просто уникальность. | Гибче, когда нужно считать, суммировать, находить мин/макс по группам. |

    ---

    💡 Пример:

    ```sql
    -- DISTINCT
    SELECT DISTINCT city FROM customers;

    -- GROUP BY
    SELECT city, COUNT(*) FROM customers GROUP BY city;
    ```

    Первый просто вернёт список городов без повторений, второй — список городов с количеством клиентов в каждом.

    ---

</details>

4. <details><summary>Как ограничить количество строк в ответе?</summary>

    Чтобы ограничить количество строк, которые возвращает SQL-запрос, используют ключевое слово **`LIMIT`** (в большинстве СУБД, например PostgreSQL, MySQL, SQLite) или **`TOP`** (в SQL Server).

    ---

    ### Пример с `LIMIT`:

    ```sql
    SELECT *
    FROM customers
    LIMIT 10;
    ```

    Вернёт только первые 10 строк результата.

    Можно также указать смещение (offset) для постраничного вывода:

    ```sql
    SELECT *
    FROM customers
    LIMIT 10 OFFSET 20;
    ```

    Это вернёт строки с 21-й по 30-ю.

    ---

    ### SQL Server:

    ```sql
    SELECT TOP 10 *
    FROM customers;
    ```

    Возвращает первые 10 строк.

    ---

    💡 Используют для:

    * Пагинации (разбиения на страницы)
    * Ограничения выборки для тестов
    * Оптимизации при выборке большого объёма данных

    ---

</details>

5. <details><summary>Как отфильтровать строки по диапазону значений?</summary>

    В SQL строки по диапазону значений фильтруют с помощью оператора **`BETWEEN`** или комбинации **`>=`** и **`<=`** в секции `WHERE`.

    ---

    ### `BETWEEN`

    Оператор проверяет, находится ли значение **включительно** в заданных границах:

    ```sql
    SELECT *
    FROM orders
    WHERE order_date BETWEEN '2025-01-01' AND '2025-01-31';
    ```

    Вернёт все заказы за январь 2025 года (включая 1-е и 31-е число).

    ---

    ### Эквивалент через сравнения:

    ```sql
    SELECT *
    FROM orders
    WHERE order_date >= '2025-01-01'
    AND order_date <= '2025-01-31';
    ```

    ---

    💡 Особенности:

    * `BETWEEN` включает границы.
    * Можно использовать с числами, датами, строками.
    * Для строк сравнение идёт по алфавитному порядку.

    ---

    📌 Пример с числами:

    ```sql
    SELECT *
    FROM products
    WHERE price BETWEEN 100 AND 500;
    ```

    Вернёт товары с ценой от 100 до 500 включительно.

    ---

</details>

**Продвинутые:**

6. <details><summary>Что такое индекс и как он влияет на производительность?</summary>

    В **PostgreSQL** индекс — это специальная структура данных (обычно B-tree, но есть и другие типы), которая ускоряет поиск строк в таблице, аналогично оглавлению в книге. Вместо полного сканирования таблицы (**seq scan**) база использует индекс, чтобы быстро найти нужные записи.

    ---

    ### Как работает:

    * Индекс хранит упорядоченные ссылки на строки по ключевому столбцу(ам).
    * При запросе с условием `WHERE` или при сортировке (`ORDER BY`) PostgreSQL может обратиться к индексу, а не к полному набору данных.
    * Это уменьшает количество чтений с диска и ускоряет выполнение запросов, особенно на больших таблицах.

    ---

    ### Пример:

    ```sql
    CREATE INDEX idx_users_email ON users(email);

    SELECT * FROM users WHERE email = 'test@example.com';
    ```

    После создания индекса поиск по `email` будет работать быстрее.

    ---

    ### Влияние на производительность:

    * **Плюсы:** Быстрый поиск, ускоренная сортировка и фильтрация.
    * **Минусы:** Индексы занимают место на диске и замедляют операции `INSERT`, `UPDATE`, `DELETE`, так как их тоже нужно обновлять.

    ---

    💡 В PostgreSQL важно анализировать запросы (`EXPLAIN ANALYZE`), чтобы убедиться, что индекс действительно используется.

    ---

</details>

7. <details><summary>Разница между `UNION` и `UNION ALL`.</summary>

    В SQL (и в PostgreSQL в частности)

    * **`UNION`** объединяет результаты двух (или более) запросов, **удаляя дубликаты**.
    * **`UNION ALL`** объединяет результаты, **сохраняя все строки**, даже если они повторяются.

    ---

    ### Пример

    ```sql
    SELECT 'A' AS letter
    UNION
    SELECT 'A' AS letter;
    -- Результат: 1 строка ("A")

    SELECT 'A' AS letter
    UNION ALL
    SELECT 'A' AS letter;
    -- Результат: 2 строки ("A", "A")
    ```

    ---

    ### Разница в производительности

    * `UNION` медленнее, так как требует дополнительной сортировки/сравнения для удаления дубликатов.
    * `UNION ALL` быстрее, потому что просто склеивает результаты без фильтрации.

    ---

    💡 Рекомендация: использовать `UNION` **только если** нужны уникальные строки; в остальных случаях — `UNION ALL` для лучшей производительности.

    ---

</details>

8. <details><summary>Как работают транзакции в SQL?</summary>

    В SQL транзакция — это **последовательность операций**, которые выполняются как единое целое: либо **все изменения фиксируются**, либо **отменяются полностью**.
    Это помогает поддерживать целостность данных.

    ---

    ### 🔑 Принципы (ACID)

    1. **Atomicity (Атомарность)** — транзакция либо выполняется полностью, либо не выполняется вообще.
    2. **Consistency (Согласованность)** — после транзакции данные должны оставаться в корректном состоянии.
    3. **Isolation (Изоляция)** — параллельные транзакции не мешают друг другу.
    4. **Durability (Долговечность)** — после фиксации (`COMMIT`) изменения сохраняются даже при сбоях.

    ---

    ### 📌 Основные команды

    ```sql
    BEGIN;       -- Начало транзакции
    UPDATE accounts SET balance = balance - 100 WHERE id = 1;
    UPDATE accounts SET balance = balance + 100 WHERE id = 2;
    COMMIT;      -- Зафиксировать изменения

    -- Если что-то пошло не так:
    ROLLBACK;    -- Отменить все изменения
    ```

    ---

    ### 🛠 Применение

    * Перевод денег между счетами
    * Массовые обновления или удаления
    * Операции, где критична целостность данных

    ---

    💡 В PostgreSQL каждая команда по умолчанию выполняется в собственной транзакции,
    но для нескольких связанных операций нужно явно использовать `BEGIN ... COMMIT`.

    ---

</details>

9. <details><summary>Что такое `CTE` (Common Table Expressions)?</summary>

    **CTE (Common Table Expression)** — это *временный именованный результат запроса*, который можно использовать внутри основного SQL-запроса.
    Он создаётся с помощью ключевого слова `WITH` и существует **только во время выполнения** запроса.

    ---

    ### 📌 Пример

    ```sql
    WITH recent_orders AS (
        SELECT id, customer_id, total
        FROM orders
        WHERE order_date > CURRENT_DATE - INTERVAL '7 days'
    )
    SELECT customer_id, SUM(total) AS weekly_total
    FROM recent_orders
    GROUP BY customer_id;
    ```

    Здесь `recent_orders` — это CTE, который мы переиспользуем в основном запросе.

    ---

    ### 🔹 Преимущества

    1. **Повышает читаемость кода** — сложные подзапросы можно вынести в именованные блоки.
    2. **Повторное использование** — один CTE можно использовать несколько раз в запросе.
    3. **Рекурсивные запросы** — CTE поддерживает рекурсию, например для работы с иерархическими структурами.

    ---

    ### 🛠 Рекурсивный пример

    ```sql
    WITH RECURSIVE subordinates AS (
        SELECT id, manager_id, name
        FROM employees
        WHERE id = 1 -- стартовый сотрудник
        UNION ALL
        SELECT e.id, e.manager_id, e.name
        FROM employees e
        JOIN subordinates s ON e.manager_id = s.id
    )
    SELECT * FROM subordinates;
    ```

    Этот запрос достанет всех подчинённых сотрудника с `id = 1`.

    ---

    💡 В PostgreSQL CTE по умолчанию *материализуется* (выполняется отдельно),
    но с версии 12 можно использовать `WITH ... MATERIALIZED / NOT MATERIALIZED` для оптимизации.

    ---

</details>

10. <details><summary>Как работает оконная функция `ROW_NUMBER()` и для чего она нужна?</summary>

    **`ROW_NUMBER()`** — это оконная функция в SQL, которая нумерует строки в порядке, определённом выражением `ORDER BY` внутри конструкции `OVER(...)`. Нумерация начинается с 1 и идёт без пропусков.

    Синтаксис:

    ```sql
    ROW_NUMBER() OVER (
        PARTITION BY column -- необязательно
        ORDER BY column
    )
    ```

    * `PARTITION BY` делит набор данных на группы, в каждой нумерация начинается заново.
    * `ORDER BY` задаёт порядок, по которому присваиваются номера.

    **Пример:**

    ```sql
    SELECT department, name, salary,
        ROW_NUMBER() OVER (
            PARTITION BY department
            ORDER BY salary DESC
        ) AS rank
    FROM employees;
    ```

    Этот запрос даст нумерацию сотрудников внутри каждого отдела по убыванию зарплаты.

    **Применение:**

    * выбор топ-N строк в группе (например, 3 лучших по продажам);
    * удаление дубликатов, оставляя одну запись по правилу;
    * реализация постраничной выдачи (pagination).

    ---

</details>

**Коварные:**

11. <details><summary>Как избежать проблемы **N+1** при работе с ORM?</summary>

    **Проблема N+1** возникает, когда при получении списка объектов ORM для каждой записи делает отдельный SQL-запрос для связанных данных. Например: 1 запрос на список пользователей + N запросов на их заказы.

    **Как избежать:**

    1. **Жадная загрузка (eager loading)** — заранее подгружать связанные данные одним запросом с `JOIN` или подзапросами.

    * В Diesel: `.inner_join()` или `.belonging_to(...).load(...)` с `grouped_by`.
    * В других ORM: `preload`, `include`, `select_related`.
    2. **Использовать `IN` вместо множества `SELECT`** — загружать все связанные объекты сразу по списку ключей.
    3. **Кэширование** — если данные часто повторяются, кэшировать результаты.
    4. **Анализ запросов** — включить лог SQL в ORM, чтобы выявлять N+1.

    📌 Идея: минимизировать количество SQL-запросов, объединяя данные заранее.

    ---

</details>

12. <details><summary>Что произойдёт при удалении строки, на которую ссылается внешний ключ?</summary>

    В **PostgreSQL** (и других СУБД) поведение при удалении строки, на которую ссылается внешний ключ (`FOREIGN KEY`), зависит от политики, заданной в ограничении:

    * **`ON DELETE RESTRICT`** *(по умолчанию)* — удаление запрещено, пока существуют связанные строки. Бросается ошибка.
    * **`ON DELETE CASCADE`** — вместе с родительской строкой удаляются все дочерние записи.
    * **`ON DELETE SET NULL`** — в дочерних строках поле с внешним ключом обнуляется (`NULL`).
    * **`ON DELETE SET DEFAULT`** — устанавливается значение по умолчанию.
    * **`NO ACTION`** — как `RESTRICT`, но проверка происходит в конце транзакции.

    📌 По умолчанию удаление блокируется, чтобы сохранить целостность данных. Нужно явно указать нужное поведение при создании внешнего ключа.

    ---

</details>

13. <details><summary>Что делает `EXPLAIN` и `EXPLAIN ANALYZE`?</summary>

    В **PostgreSQL** (и других СУБД) команды `EXPLAIN` и `EXPLAIN ANALYZE` применяются для анализа производительности SQL-запросов:

    * **`EXPLAIN`** — показывает *план выполнения запроса*, который оптимизатор собирается использовать: какие индексы будут задействованы, порядок соединений таблиц, метод сканирования (последовательный, индексный и т. д.), предполагаемое количество строк и стоимость операций. Запрос фактически **не выполняется**.

    * **`EXPLAIN ANALYZE`** — выполняет запрос **и** показывает фактические результаты: реальное время выполнения, количество реально обработанных строк на каждом шаге, отличие от прогнозов оптимизатора. Это помогает находить узкие места.

    📌 Используется для оптимизации запросов: помогает понять, где нужен индекс, как переписать `JOIN` или изменить фильтры.

    ---

</details>

---

## 🐘 **PostgreSQL**

**Базовые:**

1. <details><summary>Чем PostgreSQL отличается от MySQL?</summary>

    **PostgreSQL vs MySQL — ключевые отличия:**

    1. **Архитектура и философия**

    * **PostgreSQL** — объектно-реляционная СУБД, акцент на расширяемость, стандарты SQL, сложные запросы.
    * **MySQL** — чисто реляционная СУБД, ориентирована на скорость и простоту, исторически популярна в веб-приложениях.

    2. **Поддержка стандартов SQL**

    * PostgreSQL строже следует SQL-стандартам, поддерживает CTE, оконные функции, рекурсивные запросы, богатый набор типов данных (JSONB, массивы, hstore).
    * MySQL отстает по стандартам (например, CTE и оконные функции появились только в версии 8.0).

    3. **Типы данных**

    * PostgreSQL: расширяемый набор типов (можно создавать свои), JSONB с индексами, геоданные через PostGIS.
    * MySQL: JSON только как текст (без индексов до 8.0), ограниченный набор типов.

    4. **Транзакции и целостность данных**

    * PostgreSQL всегда работает с ACID и MVCC, поддерживает сложные транзакции, полнофункциональные внешние ключи, `CHECK`-ограничения.
    * MySQL — поведение зависит от движка (InnoDB — ACID, MyISAM — нет транзакций). Некоторые ограничения (например, `CHECK`) исторически игнорировались.

    5. **Производительность**

    * MySQL быстрее в простых SELECT’ах и чтении при высоких нагрузках.
    * PostgreSQL обычно выигрывает в сложных аналитических запросах, агрегациях и работе с большими объемами данных.

    6. **Расширяемость**

    * PostgreSQL позволяет писать функции на разных языках (PL/pgSQL, Python, C и др.), подключать кастомные индексы и типы данных.
    * MySQL менее гибкий в расширениях.

    📌 **Вывод**:

    * Если нужны сложные запросы, строгая целостность данных, аналитика и расширяемость — **PostgreSQL**.
    * Если приоритет — скорость в простых веб-операциях, минимальная настройка и совместимость с популярными хостингами — **MySQL**.

    ---

</details>

2. <details><summary>Как создать индекс и как его удалить?</summary>

    В **PostgreSQL** индексы создаются и удаляются с помощью команд `CREATE INDEX` и `DROP INDEX`.

    **Создание индекса**

    ```sql
    CREATE INDEX idx_users_name ON users(name);
    ```

    * `idx_users_name` — имя индекса.
    * `users` — таблица.
    * `name` — колонка, по которой строится индекс.

    **Частичный индекс (для выборки по условию)**

    ```sql
    CREATE INDEX idx_users_active ON users(name) WHERE active = true;
    ```

    **Удаление индекса**

    ```sql
    DROP INDEX idx_users_name;
    ```

    **Ключевые моменты:**

    * Индексы ускоряют `SELECT`, но замедляют `INSERT`/`UPDATE`/`DELETE` (так как нужно обновлять индекс).
    * Перед созданием стоит проверить план запроса (`EXPLAIN`) — возможно, индекс не нужен.
    * Для полнотекстового поиска используют `GIN`/`GiST` индексы:

    ```sql
    CREATE INDEX idx_posts_content ON posts USING GIN(to_tsvector('english', content));
    ```

    ---

</details>

3. <details><summary>Что такое `SERIAL` и `BIGSERIAL`?</summary>

    В **PostgreSQL** `SERIAL` и `BIGSERIAL` — это удобные типы для создания автоинкрементных целых колонок.

    * **`SERIAL`** → 4-байтовое целое (`INTEGER`), диапазон примерно до ±2 млрд.
    * **`BIGSERIAL`** → 8-байтовое целое (`BIGINT`), диапазон примерно до ±9 квинтиллионов.

    При создании такой колонки:

    ```sql
    id SERIAL PRIMARY KEY
    ```

    Postgres автоматически:

    1. Создаёт колонку типа `INTEGER` или `BIGINT`.
    2. Создаёт **sequence** (счётчик).
    3. Привязывает колонку к этой sequence для автоинкремента.

    Разница в том, что **`BIGSERIAL`** используется, когда ожидается очень много записей (миллиарды+).

    ---

</details>

4. <details><summary>Как выполнить полнотекстовый поиск в PostgreSQL?</summary>

    В **PostgreSQL** полнотекстовый поиск реализуется через типы `tsvector` и `tsquery`.

    **Принцип работы:**

    * `tsvector` — нормализованный текст (разбит на лексемы, без стоп-слов).
    * `tsquery` — поисковый запрос в формате лексем.
    * Сравнение выполняется оператором `@@`.

    **Пример:**

    ```sql
    SELECT *
    FROM articles
    WHERE to_tsvector('russian', content) @@ to_tsquery('russian', 'rust & ownership');
    ```

    Здесь:

    * `'rust & ownership'` означает, что в тексте должны быть оба слова.
    * `'russian'` — используемый словарь (есть и другие: `'english'`, `'simple'`).

    **Оптимизация:**

    * Создать GIN-индекс:

    ```sql
    CREATE INDEX idx_articles_search
    ON articles USING GIN (to_tsvector('russian', content));
    ```

    * Это ускорит поиск в больших таблицах.

    **Дополнительно:** можно использовать `plainto_tsquery` (автоматическая разбивка текста) или `websearch_to_tsquery` (поиск как в Google).

    ---

</details>

5. <details><summary>Как добавить новый столбец в таблицу?</summary>

    В **PostgreSQL** новый столбец добавляется командой:

    ```sql
    ALTER TABLE table_name ADD COLUMN column_name data_type;
    ```

    **Пример:**

    ```sql
    ALTER TABLE users ADD COLUMN last_login TIMESTAMP;
    ```

    * `ADD COLUMN` — добавляет поле.
    * Можно указать `DEFAULT` и `NOT NULL`, если нужно сразу задать значение и ограничение.

    ---

</details>

**Продвинутые:**

6. <details><summary>Что такое `JSONB` и как с ним работать?</summary>

    В **PostgreSQL** `JSONB` — это бинарный формат хранения JSON-документов.
    В отличие от обычного `JSON`, он:

    * хранится в разобранном виде (быстрее читать и фильтровать),
    * поддерживает индексацию (GIN, GiST),
    * игнорирует порядок ключей и дубликаты.

    **Создание и вставка:**

    ```sql
    CREATE TABLE products (id SERIAL, data JSONB);

    INSERT INTO products (data)
    VALUES ('{"name": "Phone", "price": 500}');
    ```

    **Доступ к полям:**

    ```sql
    SELECT data->>'name' AS product_name FROM products;
    ```

    **Фильтрация:**

    ```sql
    SELECT * FROM products
    WHERE data @> '{"price": 500}';
    ```

    **Индексация для ускорения поиска:**

    ```sql
    CREATE INDEX idx_products_data ON products USING GIN (data);
    ```

    > `JSONB` удобен для полуструктурированных данных, но при больших объемах стоит тщательно продумывать индексацию.

    ---

</details>

7. <details><summary>Разница между `VACUUM`, `VACUUM FULL` и `ANALYZE`.</summary>

    В PostgreSQL:

    * **`VACUUM`** — удаляет “мёртвые” строки (версии записей, удалённые или обновлённые), освобождая место для повторного использования, **без** физического уменьшения размера файла. Не блокирует чтение/запись.

    * **`VACUUM FULL`** — делает то же, что `VACUUM`, но **физически** сжимает файлы таблиц, освобождая диск. Требует эксклюзивной блокировки таблицы, работает дольше.

    * **`ANALYZE`** — собирает статистику по содержимому таблиц (распределение значений, количество строк и т. д.) для оптимизатора запросов. Не удаляет данные.

    **Комбинированный вариант:**

    ```sql
    VACUUM ANALYZE table_name;
    ```

    — чистит “мусор” и обновляет статистику.

    > Обычно `VACUUM` выполняется авто-демоном, `VACUUM FULL` — вручную при фрагментации, `ANALYZE` — после массовых изменений данных.

    ---

</details>

8. <details><summary>Как настроить репликацию в PostgreSQL?</summary>

    В PostgreSQL репликация обычно настраивается в режиме **streaming replication**, где один сервер — **primary** (мастер), а другие — **replica** (слейвы), синхронные или асинхронные.

    ---

    ### 1. Включаем репликацию на primary

    В `postgresql.conf`:

    ```conf
    wal_level = replica       # уровень журналирования
    max_wal_senders = 10      # макс. число процессов-отправителей
    wal_keep_size = 64        # объем WAL для хранения (MB)
    archive_mode = on         # для архивации (опц.)
    ```

    В `pg_hba.conf` разрешаем подключение реплик:

    ```
    host replication repl_user 192.168.1.0/24 md5
    ```

    Перезапускаем сервер.

    ---

    ### 2. Создаём пользователя для репликации

    ```sql
    CREATE ROLE repl_user REPLICATION LOGIN ENCRYPTED PASSWORD 'secret';
    ```

    ---

    ### 3. Подготавливаем данные для реплики

    Останавливаем PostgreSQL на реплике и выполняем:

    ```bash
    pg_basebackup -h primary_host -D /var/lib/postgresql/data \
    -U repl_user -P --wal-method=stream
    ```

    ---

    ### 4. Настраиваем реплику

    В `postgresql.conf` на реплике:

    ```conf
    hot_standby = on
    ```

    Создаём `standby.signal` в data-dir и указываем подключение в `primary_conninfo`:

    ```conf
    primary_conninfo = 'host=primary_host port=5432 user=repl_user password=secret'
    ```

    ---

    ### 5. Запускаем реплику

    После запуска она будет получать WAL-записи с primary.

    ---

    **Виды репликации:**

    * **Асинхронная** — реплика может отставать, но быстрее.
    * **Синхронная** — транзакция на primary считается завершённой только после подтверждения репликой (меньше риск потери данных, но медленнее).

    ---

</details>

9. <details><summary>Что такое материализованное представление (`MATERIALIZED VIEW`)?</summary>

    **Материализованное представление (MATERIALIZED VIEW)** в PostgreSQL — это объект базы данных, который хранит **результат запроса** как обычную таблицу.

    В отличие от обычного `VIEW` (который каждый раз выполняет SQL-запрос при обращении), `MATERIALIZED VIEW` сохраняет данные на диске, и их можно читать без повторного вычисления.

    ---

    ### Зачем использовать

    * Ускоряет сложные запросы (особенно с агрегациями и join’ами).
    * Уменьшает нагрузку на базу за счёт предрасчитанных данных.

    ---

    ### Пример

    ```sql
    CREATE MATERIALIZED VIEW top_customers AS
    SELECT customer_id, SUM(amount) AS total_spent
    FROM orders
    GROUP BY customer_id;
    ```

    Чтение:

    ```sql
    SELECT * FROM top_customers;
    ```

    ---

    ### Обновление

    Данные в `MATERIALIZED VIEW` **не обновляются автоматически**.
    Нужно вручную вызывать:

    ```sql
    REFRESH MATERIALIZED VIEW top_customers;
    ```

    Можно добавить `CONCURRENTLY`, чтобы во время обновления представление оставалось доступным:

    ```sql
    REFRESH MATERIALIZED VIEW CONCURRENTLY top_customers;
    ```

    (но для этого нужен индекс по уникальному ключу).

    ---

    💡 По сути, это как **кэш** сложного запроса в таблице, который вы сами обновляете.

    ---

</details>

10. <details><summary>Как работает блокировка строк (`FOR UPDATE`, `FOR SHARE`)?</summary>

    В PostgreSQL блокировки строк (`FOR UPDATE`, `FOR SHARE`) используются для **управления конкурентным доступом** к данным внутри транзакций, чтобы избежать гонок данных.

    ---

    ### 🔹 `FOR UPDATE`

    * **Назначение:** блокирует выбранные строки для изменения другими транзакциями.
    * Пока блокировка держится, **другие транзакции**:

    * Не могут изменить эти строки (`UPDATE`, `DELETE`).
    * Но могут их читать (`SELECT` без блокировки).
    * Пример:

    ```sql
    BEGIN;
    SELECT * FROM accounts WHERE id = 1 FOR UPDATE;
    -- делаем изменения
    UPDATE accounts SET balance = balance - 100 WHERE id = 1;
    COMMIT;
    ```

    Если другая транзакция попытается сделать `UPDATE` той же строки, она **подождёт**, пока первая не завершится.

    ---

    ### 🔹 `FOR SHARE`

    * **Назначение:** ставит **разделяемую блокировку** — защищает строки от изменений, но позволяет другим транзакциям ставить такие же `FOR SHARE`.
    * Используется, когда нужно **прочитать данные и быть уверенным, что их никто не изменит до конца транзакции**.
    * Пример:

    ```sql
    BEGIN;
    SELECT * FROM accounts WHERE id = 1 FOR SHARE;
    COMMIT;
    ```

    ---

    💡 В веб-разработке `FOR UPDATE` часто используют для **реализации транзакционного “SELECT + UPDATE”**, например, при бронировании билетов, чтобы два пользователя не купили одно и то же место.

    ---

</details>

---

## 🐳 **Docker**

**Базовые:**

1. <details><summary>Разница между образом (`image`) и контейнером (`container`).</summary>

    **Образ (image)** — это **шаблон** с упакованным приложением и его зависимостями. Он неизменяемый и используется как “чертёж” для запуска контейнеров.

    **Контейнер (container)** — это **запущенный экземпляр образа**, изолированная среда, в которой приложение реально работает. Контейнер можно запускать, останавливать и удалять, а образ при этом остаётся неизменным.

    📌 Аналогия:

    * Образ — **рецепт** торта.
    * Контейнер — **готовый торт**, сделанный по рецепту.

    ---

</details>

2. <details><summary>Что такое `Dockerfile` и для чего он нужен?</summary>

    **Dockerfile** — это текстовый файл с инструкциями, описывающими, как собрать Docker-образ.

    В нём указывают:

    * базовый образ (`FROM`),
    * команды для установки зависимостей (`RUN`),
    * копирование файлов (`COPY`, `ADD`),
    * настройку переменных окружения (`ENV`),
    * команду запуска (`CMD` или `ENTRYPOINT`).

    📌 По сути, это **чек-лист для повара**, по которому Docker “печёт” образ.

    ---

</details>

3. <details><summary>Как пробросить порт из контейнера наружу?</summary>

    Чтобы пробросить порт из контейнера в хост, при запуске указывают флаг **`-p`** в формате:

    ```bash
    docker run -p <порт_хоста>:<порт_контейнера> my_app
    ```

    Например:

    ```bash
    docker run -p 8080:80 nginx
    ```

    📌 Здесь порт `80` внутри контейнера (где слушает Nginx) будет доступен на `localhost:8080` на хосте.

    Можно пробрасывать несколько портов, повторяя `-p`.

    ---

</details>

4. <details><summary>Как смонтировать папку с хоста в контейнер?</summary>

    Чтобы смонтировать папку с хоста в контейнер, при запуске используют флаг **`-v`** или более современный **`--mount`**.

    **Пример с `-v`:**

    ```bash
    docker run -v /путь/на/хосте:/путь/в/контейнере my_app
    ```

    Например:

    ```bash
    docker run -v $(pwd)/data:/app/data my_app
    ```

    📌 Здесь папка `data` из текущей директории хоста будет доступна в контейнере по пути `/app/data`.

    **Пример с `--mount` (рекомендуется):**

    ```bash
    docker run --mount type=bind,source=$(pwd)/data,target=/app/data my_app
    ```

    Это более явная запись, которая удобнее для сложных конфигураций.

    ---

</details>

5. <details><summary>Как посмотреть логи контейнера?</summary>

    Чтобы посмотреть логи контейнера в Docker, используется команда:

    ```bash
    docker logs <container_id_or_name>
    ```

    **Примеры:**

    ```bash
    docker logs my_app
    docker logs 123abc456def
    ```

    📌 Полезные опции:

    * `-f` — следить за логами в реальном времени (аналог `tail -f`):

    ```bash
    docker logs -f my_app
    ```
    * `--tail 100` — показать только последние 100 строк:

    ```bash
    docker logs --tail 100 my_app
    ```

    ---

</details>

**Продвинутые:**

6. <details><summary>Что такое multi-stage build и зачем он нужен?</summary>

    **Multi-stage build** в Docker — это подход, при котором в одном `Dockerfile` используется несколько последовательных стадий сборки, и результат каждой стадии можно передавать в следующую.

    💡 **Зачем нужен**
    Главная цель — **уменьшить размер конечного образа** и **отделить этапы сборки от этапа запуска**.
    Обычно это полезно, когда:

    * Для сборки нужны тяжёлые зависимости (компиляторы, SDK, dev-библиотеки).
    * Для работы приложения нужны только готовые бинарники/артефакты.

    ---

    ### 📌 Как это работает

    Вместо того чтобы собирать и запускать всё в одном контейнере, мы:

    1. **Сначала** используем «тяжёлый» базовый образ (например, `rust:latest` или `golang:alpine`) для сборки.
    2. **Затем** переносим готовый артефакт в «лёгкий» базовый образ (например, `alpine`), где он будет запускаться.

    ---

    ### 🔹 Пример (Rust + multi-stage)

    ```dockerfile
    # 1️⃣ Стадия сборки
    FROM rust:1.70 as builder
    WORKDIR /app
    COPY . .
    RUN cargo build --release

    # 2️⃣ Стадия запуска
    FROM debian:bullseye-slim
    WORKDIR /app
    COPY --from=builder /app/target/release/my_app .
    CMD ["./my_app"]
    ```

    ---

    ### ✅ Преимущества

    * **Меньший размер** итогового образа (не тащим весь компилятор и зависимости в прод).
    * **Повышенная безопасность** (в финальном контейнере нет лишних инструментов).
    * **Чистота** — финальный образ содержит только то, что нужно для работы.

    ---

</details>

7. <details><summary>Как уменьшить размер Docker-образа?</summary>

    Коротко:

    1. **Использовать лёгкие базовые образы** (`alpine`, `debian:slim`).
    2. **Multi-stage build** — отделить сборку от запуска.
    3. **Удалять кэш и временные файлы** (`rm -rf /var/lib/apt/lists/*`).
    4. **Минимизировать слои** — объединять команды в `RUN`.
    5. **Копировать только нужные файлы** (`.dockerignore`).
    6. **Использовать сжатие** (`docker buildx build --platform ... --compress`).

    ---

</details>

8. <details><summary>Как работают сети в Docker?</summary>

    В Docker сети позволяют контейнерам общаться между собой и с внешним миром.

    Основные типы:

    1. **bridge** (по умолчанию) — каждый контейнер получает свой IP в виртуальной сети, связь между ними через имя контейнера.
    2. **host** — контейнер использует сетевой стек хоста (без изоляции портов).
    3. **none** — полностью отключена сеть.
    4. **overlay** — сеть для связи контейнеров между разными хостами (Swarm, Kubernetes).

    Механизм:

    * Docker создаёт виртуальные интерфейсы и мосты (`docker0`) в системе.
    * Контейнеры подключаются к этим мостам через виртуальные Ethernet-кабели (veth-пары).
    * Маршрутизация и NAT обеспечивают доступ в интернет и извне.

    Настройка:

    * `docker network ls` — список сетей.
    * `docker network create` — создание кастомной сети.
    * Контейнеры в одной сети видят друг друга по DNS-имени контейнера.

    ---

</details>

9. <details><summary>Как настроить `docker-compose` для нескольких сервисов?</summary>

    В `docker-compose` можно описать несколько сервисов в одном `docker-compose.yml`, чтобы они автоматически запускались и были соединены в общую сеть.

    Пример:

    ```yaml
    version: "3.9"
    services:
    web:
        build: ./web
        ports:
        - "8080:80"
        depends_on:
        - db

    db:
        image: postgres:15
        environment:
        POSTGRES_USER: user
        POSTGRES_PASSWORD: pass
        volumes:
        - db_data:/var/lib/postgresql/data

    volumes:
    db_data:
    ```

    Ключи:

    * **services** — список контейнеров.
    * **depends\_on** — порядок запуска (web после db).
    * **volumes** — хранение данных между перезапусками.
    * По умолчанию все сервисы подключены к одной сети и могут общаться по имени сервиса (`db:5432`).

    Запуск:

    ```bash
    docker-compose up -d
    ```

    Останов:

    ```bash
    docker-compose down
    ```

    ---

</details>

10. <details><summary>Как обновить контейнер без потери данных?</summary>

    Чтобы обновить контейнер без потери данных, нужно хранить важную информацию вне контейнера — в **томах (volumes)** или **смонтированных папках (bind mounts)**.

    Процесс:

    1. **Данные в volume** — при создании контейнера подключаем:

    ```bash
    docker run -d \
        -v my_data:/var/lib/postgresql/data \
        postgres:15
    ```

    Volume (`my_data`) сохранит файлы, даже если контейнер удалить.

    2. **Обновление контейнера**:

    ```bash
    docker pull postgres:15    # скачиваем новую версию
    docker stop my_container
    docker rm my_container
    docker run -d \
        --name my_container \
        -v my_data:/var/lib/postgresql/data \
        postgres:15
    ```

    Так мы создаём новый контейнер с теми же данными.

    3. Для `docker-compose`:

    ```bash
    docker-compose pull
    docker-compose up -d
    ```

    При этом данные сохранятся, если в `docker-compose.yml` они подключены как `volumes`.

    💡 Главное — **никогда не хранить данные внутри контейнера**, иначе при пересоздании они будут потеряны.

    ---

</details>


---

## 🌿 **Git**

**Базовые:**

1. <details><summary>Как создать новую ветку и переключиться на неё?</summary>

    В Git создать новую ветку и сразу на неё переключиться можно так:

    ```bash
    git checkout -b имя_ветки
    ```

    или в новой версии Git:

    ```bash
    git switch -c имя_ветки
    ```

    Обе команды создают ветку и делают её текущей.

    ---

</details>

2. <details><summary>Разница между `git merge` и `git rebase`.</summary>

    **`git merge`** и **`git rebase`** — это два способа объединить изменения из одной ветки в другую, но они делают это по-разному.

    * **`git merge`** — создает *merge commit*, сохраняя историю обеих веток. История получается «разветвлённой» и наглядно показывает, когда происходило слияние. Безопаснее для командной работы, так как не переписывает историю.

    ```bash
    git checkout main
    git merge feature
    ```

    * **`git rebase`** — «переписывает» коммиты ветки так, будто они были сделаны поверх другой ветки. История получается линейной, без merge commit’ов, но меняются хэши коммитов. Опасно применять к веткам, которые уже отправлены в общий репозиторий.

    ```bash
    git checkout feature
    git rebase main
    ```

    📌 Итог:

    * **merge** — сохраняет историю, удобен для общего репо.
    * **rebase** — делает историю линейной, удобно для чистоты локальной истории.

    ---

</details>

3. <details><summary>Как отменить последний коммит?</summary>

    Отменить последний коммит в Git можно несколькими способами — в зависимости от того, хотите ли вы сохранить изменения в файлах или полностью их убрать.

    1. **Отменить коммит, но оставить изменения в рабочей директории** (например, чтобы поправить файлы и закоммитить снова):

    ```bash
    git reset --soft HEAD~1
    ```

    2. **Отменить коммит и вернуть изменения в *staging area***:

    ```bash
    git reset --mixed HEAD~1
    ```

    *(по умолчанию — то же, что `git reset HEAD~1`)*

    3. **Отменить коммит и полностью убрать изменения** (осторожно — потеряете несохранённые изменения):

    ```bash
    git reset --hard HEAD~1
    ```

    📌 Если коммит уже отправлен в удалённый репозиторий, то для его отмены понадобится
    `git push --force`, что может переписать историю и затронуть работу других.

    ---

</details>

4. <details><summary>Что делает `git stash`?</summary>

    `git stash` — временно сохраняет незакоммиченные изменения (включая файлы в **staging** и рабочей директории) в специальное хранилище и откатывает рабочее дерево к чистому состоянию.

    Полезно, когда нужно переключиться на другую ветку или сделать срочный фикс, не теряя текущую работу.

    Пример:

    ```bash
    git stash        # сохранить изменения
    git stash pop    # восстановить и удалить из stash
    git stash apply  # восстановить, но оставить копию в stash
    ```

    ---

</details>

5. <details><summary>Как посмотреть историю коммитов?</summary>

    Историю коммитов можно посмотреть командой:

    ```bash
    git log
    ```

    Полезные варианты:

    * `git log --oneline` — краткий список.
    * `git log --graph --oneline --decorate` — визуализация веток.
    * `git log -p` — с показом изменений в каждом коммите.

    ---

</details>

**Продвинутые:**

6. <details><summary>Что такое fast-forward merge?</summary>

    **Fast-forward merge** в Git — это слияние, при котором ветка назначения просто "перематывается" вперёд до нужного коммита без создания нового merge-коммита.

    📌 Условия:

    * Ветка назначения не имеет собственных уникальных коммитов.
    * История веток линейная, без расхождений.

    🔍 Пример:

    ```bash
    # main → A---B
    # feature → A---B---C---D

    git checkout main
    git merge feature
    # main просто указывает на D
    ```

    ✅ Плюсы: чистая линейная история.
    ⚠️ Минусы: может быть сложно понять, когда именно была объединена ветка.

    ---

</details>

7. <details><summary>Как исправить коммит в середине истории?</summary>

    Чтобы исправить коммит в **середине истории** в Git, используют **интерактивный rebase**:

    ---

    ### 🔧 Пошагово:

    1. Определите хэш коммита, который хотите исправить:

    ```bash
    git log --oneline
    ```

    2. Запустите интерактивный rebase на нужную глубину:

    ```bash
    git rebase -i <hash_предыдущего_коммита>
    ```

    > `<hash_предыдущего_коммита>` — это коммит перед тем, который нужно изменить.

    3. В открывшемся редакторе замените `pick` на:

    * `edit` (если хотите изменить сам коммит)
    * `reword` (если нужно только поменять сообщение).

    4. Если выбрали `edit`:

    * Внесите изменения в файлы.
    * Добавьте их в индекс:

        ```bash
        git add .
        ```
    * Перезапишите коммит:

        ```bash
        git commit --amend
        ```

    5. Продолжите rebase:

    ```bash
    git rebase --continue
    ```

    ---

    ⚠️ **Важно:** если коммиты уже были отправлены (`push`) в общий репозиторий, изменение истории потребует `git push --force`, что может сломать работу коллег.

    ---

</details>

8. <details><summary>Как работать с submodules?</summary>

    В **Git submodules** позволяют подключать один репозиторий внутрь другого как зависимость — как папку, которая живёт в отдельном Git-хранилище.

    ---

    ### 🔧 Основные команды и сценарий работы

    #### 1. Добавление submodule

    ```bash
    git submodule add <URL_репозитория> <путь/к/папке>
    ```

    Пример:

    ```bash
    git submodule add https://github.com/example/lib.git libs/lib
    ```

    📌 В корне создастся файл `.gitmodules` с конфигурацией.

    ---

    #### 2. Клонирование репозитория с submodules

    По умолчанию `git clone` не загружает содержимое submodules. Нужно:

    ```bash
    git clone <URL>
    git submodule update --init --recursive
    ```

    или сразу:

    ```bash
    git clone --recurse-submodules <URL>
    ```

    ---

    #### 3. Обновление submodules

    Если внешний репозиторий обновил зависимость:

    ```bash
    git submodule update --remote --merge
    ```

    ---

    #### 4. Переключение версии submodule

    Зайдите в папку submodule и используйте обычные git-команды:

    ```bash
    cd libs/lib
    git checkout <ветка/тег/хэш>
    ```

    Потом в корневом репо зафиксируйте новое состояние:

    ```bash
    git add libs/lib
    git commit -m "Update submodule lib"
    ```

    ---

    #### 5. Удаление submodule

    1. Удалите запись из `.gitmodules`.
    2. Удалите запись из `.git/config`.
    3. Удалите папку и зафиксируйте изменения:

    ```bash
    rm -rf libs/lib
    git rm --cached libs/lib
    ```

    ---

    💡 **Когда использовать submodules:**

    * Если проект зависит от другого репозитория, но этот код не должен полностью сливаться с вашим.
    * Когда хотите контролировать точную версию внешней зависимости.

    ---

</details>

9. <details><summary>Что делает `git cherry-pick`?</summary>

    `git cherry-pick` — это команда Git, которая позволяет **выбрать один или несколько конкретных коммитов из любой ветки** и применить их изменения в текущей ветке. Это не слияние всей ветки, а точечное перенесение отдельных изменений.

    ---

    ### 📌 Как работает

    Когда выполняется `git cherry-pick <хэш_коммита>`, Git берёт изменения из указанного коммита и **создаёт новый коммит** в текущей ветке с тем же содержимым, но новым хэшем.

    ---

    ### 🔹 Пример

    Есть две ветки:

    ```
    main:    A — B — C
    feature:      \ D — E
    ```

    Если находясь в `main` выполнить:

    ```bash
    git cherry-pick <hash_E>
    ```

    то в `main` появится новый коммит с изменениями из `E`, но он не будет идентичен по хэшу исходному.

    ---

    ### ⚠️ Особенности

    * Подходит для переноса отдельных фиксов или функций, не затрагивая всю ветку.
    * При конфликте Git остановится и предложит его разрешить, затем продолжить с `git cherry-pick --continue`.
    * Создаёт новый коммит, поэтому история может разойтись, если использовать в активных ветках.

    ---

</details>

10. <details><summary>Как найти коммит, который внёс ошибку (`git bisect`)?</summary>

    `git bisect` — это инструмент Git для **поиска коммита, в котором появилась ошибка**, с помощью бинарного поиска по истории. Он значительно сокращает время поиска, проверяя только часть коммитов.

    ---

    ### 📌 Как работает

    1. Запускаем поиск:

    ```bash
    git bisect start
    ```

    2. Отмечаем текущую версию как «плохую»:

    ```bash
    git bisect bad
    ```

    3. Отмечаем известный «хороший» коммит:

    ```bash
    git bisect good <хэш_коммита>
    ```

    4. Git переключит репозиторий на середину между «хорошим» и «плохим» коммитами.

    * Проверяем код: если ошибка есть → `git bisect bad`, если нет → `git bisect good`.
    5. Повторяем, пока не останется один коммит — это и есть источник ошибки.

    ---

    ### 🔹 Пример

    ```bash
    git bisect start
    git bisect bad
    git bisect good abc123
    # тестируем и помечаем good/bad
    ```

    ---

    После поиска:

    ```bash
    git bisect reset
    ```

    возвращает репозиторий в исходное состояние.

    ---

</details>

---

## 🐧 **Linux**

**Базовые:**

1. <details><summary>Как посмотреть список запущенных процессов?</summary>  

    В Linux список запущенных процессов можно просмотреть с помощью команд:

    * `ps aux` — выводит полный список процессов с деталями (PID, CPU, память, пользователь, команда).
    * `top` или `htop` — интерактивный просмотр процессов с возможностью сортировки.
    * `pgrep <имя>` — поиск конкретного процесса по имени.

    `ps` хорош для одноразового снимка состояния, а `top/htop` — для мониторинга в реальном времени.

    ---

</details>  

2. <details><summary>Как проверить использование памяти и процессора?</summary>  

    Для проверки использования ресурсов:

    * `top` — отображает загрузку CPU, RAM, swap и список процессов в реальном времени.
    * `htop` — более удобная версия `top` с цветами и навигацией.
    * `free -h` — показывает общий объём, используемую и свободную память.
    * `vmstat` — сводная статистика памяти, процессов и ввода-вывода.

    `top` и `htop` подходят для мониторинга прямо во время работы, `free` — для быстрого отчёта.

    ---

</details>  

3. <details><summary>Как изменить права доступа к файлу?</summary>  

    В Linux права доступа к файлу меняют командой `chmod`:

    * Символьный вид: `chmod u+x script.sh` — добавить владельцу право на выполнение.
    * Цифровой вид: `chmod 755 file.txt` — владелец: чтение/запись/выполнение, группа и остальные: чтение/выполнение.

    Можно также изменить владельца файла командой `chown user:group file.txt`. Права влияют на то, кто может читать, изменять или запускать файл.

    ---

</details>  

4. <details><summary>Что делает команда `grep`?</summary>  

    `grep` ищет строки в тексте, соответствующие шаблону (регулярному выражению).
    Пример:

    ```bash
    grep "error" logfile.txt
    ```

    — выводит строки, содержащие слово `error`.
    Флаги:

    * `-i` — без учёта регистра.
    * `-r` — рекурсивный поиск в папках.
    * `-n` — показывает номер строки.

    `grep` часто используют для фильтрации логов или вывода команд, например:

    ```bash
    ps aux | grep nginx
    ```

    ---

</details>  

5. <details><summary>Как вывести последние 100 строк лога?</summary>  

    Команда `tail` позволяет вывести последние строки файла:

    ```bash
    tail -n 100 /var/log/syslog
    ```

    — выведет последние 100 строк.
    Для непрерывного просмотра изменений в реальном времени используется:

    ```bash
    tail -f /var/log/syslog
    ```

    Можно комбинировать: `tail -n 100 -f logfile.log` — показать последние 100 строк и продолжить вывод новых.

    ---

</details>  


**Продвинутые:**

6. <details><summary>Разница между `hard link` и `soft link`.</summary>  

    **Hard link** — это прямое указание на один и тот же физический файл на диске (тот же inode). Удаление исходного файла не влияет на hard link, так как данные продолжают существовать, пока есть хотя бы одна ссылка. Hard link нельзя создать на директорию и между разными файловыми системами.

    **Soft link** (symbolic link, символьная ссылка) — это отдельный файл, который указывает на путь к другому файлу. Если исходный файл удалить, soft link "сломается", так как путь станет недействительным. Soft link можно создавать между разными файловыми системами и на директории.

    ---

</details>  

7. <details><summary>Что такое `systemd` и как им управлять?</summary>  

    `systemd` — это система инициализации и менеджер сервисов в Linux, которая управляет запуском и остановкой процессов, монтированием файловых систем, логированием. Основная команда для управления — `systemctl`:

    * `systemctl start nginx` — запустить сервис.
    * `systemctl stop nginx` — остановить.
    * `systemctl restart nginx` — перезапустить.
    * `systemctl enable nginx` — включить автозапуск при старте системы.
    * `systemctl status nginx` — статус сервиса.

    `systemd` обеспечивает параллельный старт сервисов и централизованное управление.

    ---

</details>  

8. <details><summary>Как перенаправить вывод команды в файл и в консоль одновременно?</summary>  

    Для одновременного вывода в консоль и запись в файл используют команду `tee`:

    ```bash
    ls -l | tee output.txt
    ```

    — выводит список файлов на экран и сохраняет его в `output.txt`.
    Флаг `-a` добавляет данные в конец файла:

    ```bash
    ls -l | tee -a output.txt
    ```

    Можно также комбинировать с перенаправлением ошибок:

    ```bash
    command 2>&1 | tee log.txt
    ```

    ---

</details>  

9. <details><summary>Как отследить использование порта?</summary>  

    Проверить, какой процесс использует порт, можно с помощью:

    * `lsof -i :8080` — показывает процессы, использующие порт 8080.
    * `netstat -tulnp | grep 8080` — список слушающих портов и PID.
    * `ss -tulnp | grep 8080` — современная альтернатива `netstat`.

    Например, если порт занят, можно завершить процесс по PID:

    ```bash
    kill -9 <PID>
    ```

    ---

</details>  

10. <details><summary>Что такое `ulimit`?</summary>  

    `ulimit` — это команда для управления лимитами ресурсов, доступных процессам в Linux. Она может ограничивать:

    * размер файлов (`-f`),
    * количество открытых файловых дескрипторов (`-n`),
    * использование памяти (`-v`),
    * время работы CPU (`-t`).

    Пример:

    ```bash
    ulimit -n 4096
    ```

    — установить лимит в 4096 открытых файлов. Эти ограничения помогают предотвращать избыточное потребление ресурсов одной программой.

    ---

</details>  

---

## 🌐 **Web**

**Базовые:**

1. <details><summary>Разница между HTTP и HTTPS.</summary>  

    **HTTP** (HyperText Transfer Protocol) — это протокол передачи данных без шифрования. Данные между клиентом и сервером передаются в открытом виде, что делает их уязвимыми для перехвата.

    **HTTPS** (HTTP Secure) — это HTTP, работающий поверх шифрованного протокола TLS/SSL. Он обеспечивает:

    * **Шифрование** — защита данных от перехвата.
    * **Аутентификацию** — проверка подлинности сервера с помощью SSL-сертификата.
    * **Целостность данных** — предотвращение их изменения по пути.

    HTTPS обязателен для передачи паролей, платежных данных и в целом для современных сайтов.

    ---

</details>  

2. <details><summary>Что такое REST API?</summary>  

    **REST API** (Representational State Transfer) — архитектурный стиль взаимодействия клиента и сервера через HTTP.
    Принципы:

    * **Ресурсы** идентифицируются URL.
    * Действия над ресурсами выполняются с помощью HTTP-методов (`GET`, `POST`, `PUT`, `DELETE`).
    * Сервер не хранит состояние клиента (**stateless**).
    * Формат данных — чаще всего JSON.

    Пример:
    `GET /users` — получить список пользователей.
    `POST /users` — создать пользователя.

    REST прост в реализации и хорошо подходит для веб-сервисов.

    ---

</details>  

3. <details><summary>Что означают методы HTTP (`GET`, `POST`, `PUT`, `DELETE`)?</summary>  

    * **GET** — получение данных с сервера (без изменения состояния).
    * **POST** — отправка данных для создания ресурса.
    * **PUT** — полное обновление существующего ресурса.
    * **DELETE** — удаление ресурса.

    Пример:

    * `GET /users` — список пользователей.
    * `POST /users` — создать нового.
    * `PUT /users/1` — заменить пользователя с ID=1.
    * `DELETE /users/1` — удалить пользователя.

    Это базовые методы REST API, помогающие делать код понятным и структурированным.

    ---

</details>  

4. <details><summary>Что такое коды ответа 200, 301, 404, 500?</summary>  

    HTTP-коды показывают результат запроса:

    * **200 OK** — запрос выполнен успешно.
    * **301 Moved Permanently** — ресурс перемещён, нужно использовать новый URL.
    * **404 Not Found** — ресурс не найден.
    * **500 Internal Server Error** — ошибка на стороне сервера.

    Коды помогают клиенту понять, что произошло, и как реагировать. Например, 301 подскажет браузеру сделать редирект, а 500 — что проблема у сервера.

    ---

</details>  

5. <details><summary>Как работают cookies и сессии?</summary>  

    **Cookies** — это небольшие данные, которые сервер отправляет браузеру, а браузер хранит и отправляет обратно при каждом запросе к тому же домену. Используются для авторизации, хранения предпочтений.

    **Сессия** — это состояние на сервере, связанное с конкретным пользователем. Идентификатор сессии обычно хранится в cookie (`session_id`).

    Механизм:

    1. Пользователь логинится.
    2. Сервер создаёт сессию и отправляет cookie с ID.
    3. При следующих запросах браузер отправляет cookie, и сервер понимает, кто это.

    Cookies работают на клиенте, сессии — на сервере.

    ---

</details>  

**Продвинутые:**

6. <details><summary>Что такое CORS и зачем он нужен?</summary>  

    **CORS** (Cross-Origin Resource Sharing) — механизм, который определяет, может ли веб-страница делать запросы к ресурсам с другого домена.
    По умолчанию браузеры блокируют такие запросы из соображений безопасности (Same-Origin Policy).

    CORS позволяет серверу явно указать, кому разрешён доступ, с помощью HTTP-заголовков, например:
    `Access-Control-Allow-Origin: https://example.com`

    Без правильной настройки CORS, фронтенд-приложение, работающее на другом домене, не сможет взаимодействовать с API.

    ---

</details>  

7. <details><summary>Разница между WebSocket и HTTP.</summary>  

    **HTTP** — протокол запрос-ответ: клиент отправляет запрос, сервер отвечает. Подходит для одноразовых операций (REST API).

    **WebSocket** — протокол с постоянным двусторонним соединением между клиентом и сервером. Подходит для чатов, игр, онлайн-обновлений, где важна низкая задержка.

    Различия:

    * HTTP — статичный, каждый запрос открывает новое соединение.
    * WebSocket — одно соединение для обмена данными в обе стороны.
    * HTTP лучше для CRUD-запросов, WebSocket — для реального времени.

    ---

</details>  

8. <details><summary>Что такое idempotency в HTTP-запросах?</summary>  

    **Idempotency** — свойство HTTP-методов, при котором повторный запрос даёт тот же результат, что и первый (без дополнительных побочных эффектов).

    Примеры:

    * **GET** — безопасен, всегда возвращает ресурс без изменений.
    * **PUT** — обновляет ресурс, повторное выполнение не меняет результат.
    * **DELETE** — удаляет ресурс, повторный запрос не делает «дополнительного» удаления.

    Неидемпотентный метод — **POST**, т.к. при повторе он может создать дубликат.

    ---

</details>  

9. <details><summary>Как реализовать аутентификацию и авторизацию в веб-приложении?</summary>  

    * **Аутентификация** — проверка личности (логин/пароль, токен, OAuth).
    * **Авторизация** — проверка прав на выполнение действия.

    Процесс:

    1. Пользователь вводит данные для входа.
    2. Сервер проверяет их и выдает **токен** или **сессию**.
    3. Клиент отправляет токен в заголовках (`Authorization: Bearer ...`).
    4. Сервер по токену определяет пользователя и его права.

    Инструменты: JWT, OAuth2, session cookies.

    ---

</details>  

10. <details><summary>Как работает пагинация и фильтрация на уровне API?</summary>  

    **Пагинация** — разделение результатов на страницы. Реализуется параметрами:

    * `?page=2&limit=20` — страница 2, по 20 элементов.
    * `OFFSET` и `LIMIT` в SQL.

    **Фильтрация** — выборка только нужных данных. Пример:

    * `?status=active&sort=desc`

    Комбинируя пагинацию и фильтрацию, API снижает нагрузку на сервер и ускоряет работу клиента, возвращая только нужные данные порциями.

    ---

</details>  


---

# Вопросы для собеседования на **Rust** с уклоном в **ООП** и **функциональное программирование**, разделённых по темам.

---

## 🏛 ООП в Rust

Rust не является чисто ООП-языком, но поддерживает многие его концепции через **traits**, **структуры** и **инкапсуляцию**.

**Теоретические вопросы:**

1. <details><summary>Почему в Rust нет классов? Как вместо этого организуют код?</summary>  

    В Rust отсутствует классическое понятие классов из ООП, потому что язык изначально спроектирован в парадигме **"composition over inheritance"** (композиция вместо наследования) и с упором на безопасность и производительность. Вместо классов используются **структуры (`struct`)** для хранения данных и **трейты (`trait`)** для описания поведения. Такой подход позволяет явно разделять состояние и методы, избегая сложных иерархий наследования, которые могут приводить к ошибкам и затруднять поддержку кода.

    Организация кода в Rust строится через:

    * **Модули** (`mod`) для группировки логики;
    * **Пакеты и крейты** для изоляции и повторного использования;
    * **Трейты** для определения общих интерфейсов;
    * **Имплементации (`impl`)** для привязки методов к структурам или трейту.

    Вместо "наследования" поведение делается **общим через трейты**, а повторное использование кода достигается **композицией** — встраиванием структур друг в друга. Это снижает связанность и упрощает тестирование.

    ---

</details>  

2. <details><summary>Что такое `struct` и `impl` в Rust, и как они соотносятся с классами в ООП?</summary>  

    `struct` в Rust — это способ определить **тип данных с именованными полями**. Он хранит состояние, аналогично свойствам класса в ООП.

    Пример:

    ```rust
    struct User {
        name: String,
        age: u8,
    }
    ```

    `impl` — блок реализации методов для конкретного типа. Он объединяет поведение с данными, но не хранит его напрямую, как в классах.

    ```rust
    impl User {
        fn greet(&self) {
            println!("Hello, my name is {}", self.name);
        }
    }
    ```

    Сходство с классами:

    * `struct` + `impl` вместе выглядят как класс с полями и методами.

    Различия:

    * В Rust нет наследования состояния или поведения, методы добавляются только к определённому типу;
    * Можно иметь несколько `impl` для одного типа, в том числе из других модулей;
    * Поведение можно "расширять" через трейты, а не через иерархию.

    ---

</details>  

3. <details><summary>Как Rust реализует полиморфизм?</summary>  

    В Rust есть два основных способа реализовать полиморфизм:

    1. **Параметрический (через дженерики)** — компилятор подставляет конкретные типы на этапе компиляции, создавая специализированный код. Это **zero-cost abstractions**, так как нет накладных расходов времени выполнения.

    ```rust
    fn print<T: ToString>(x: T) {
        println!("{}", x.to_string());
    }
    ```

    2. **Через трейты (ад-хок полиморфизм)** — мы описываем интерфейс в виде трейта, а разные типы реализуют его по-своему. Это работает как интерфейсы в Java или C#.

    Плюсы:

    * Высокая производительность при static dispatch;
    * Чёткое разделение интерфейсов и реализаций.

    Минус:

    * Нет множественного наследования реализации, только интерфейсов (трейтов).

    ---

</details>  

4. <details><summary>Разница между **static dispatch** (`impl Trait`) и **dynamic dispatch** (`Box<dyn Trait>`).</summary>  

    **Static dispatch** (`impl Trait` или дженерики с `T: Trait`):

    * Решение, какой метод вызвать, принимается на этапе компиляции;
    * Компилятор генерирует специализированный код для каждого конкретного типа;
    * Нет дополнительных затрат времени выполнения;
    * Код становится больше (code bloat) при большом числе типов.

    **Dynamic dispatch** (`Box<dyn Trait>` или `&dyn Trait`):

    * Решение, какой метод вызвать, принимается во время выполнения;
    * Используется таблица виртуальных функций (**vtable**), как в C++;
    * Есть небольшой runtime overhead;
    * Позволяет хранить разные типы, реализующие один трейт, в одной коллекции.

    Выбор:

    * Static dispatch — для максимальной производительности;
    * Dynamic dispatch — для гибкости и работы с гетерогенными коллекциями.

    ---

</details>  

5. <details><summary>Как в Rust реализуется инкапсуляция? (модификаторы `pub`, модули).</summary>  

    В Rust **инкапсуляция** достигается через систему модулей и модификатор доступа `pub`. По умолчанию всё в модуле **приватно**, доступно только внутри него.

    Пример:

    ```rust
    mod user {
        pub struct User {
            pub name: String,
            age: u8, // приватное поле
        }

        impl User {
            pub fn new(name: String, age: u8) -> Self {
                Self { name, age }
            }
        }
    }
    ```

    * `pub struct` — структура доступна извне;
    * `pub field` — конкретное поле публично;
    * Без `pub` поле или функция доступны только внутри модуля.

    Модули (`mod`) позволяют логически разделять код, а `pub(crate)` даёт доступ внутри всего крейта, но не наружу. Это даёт точный контроль над API и защищает внутреннюю реализацию от случайного использования.

    ---

</details>  

6. <details><summary>Что такое **trait objects** и когда их использовать?</summary>  

    **Trait object** — это способ хранить значение, тип которого неизвестен на этапе компиляции, но который реализует определённый трейт. Записывается как `&dyn Trait` или `Box<dyn Trait>`.

    Пример:

    ```rust
    trait Draw {
        fn draw(&self);
    }

    struct Button;
    impl Draw for Button {
        fn draw(&self) { println!("Button"); }
    }

    struct Text;
    impl Draw for Text {
        fn draw(&self) { println!("Text"); }
    }

    fn render(ui: Vec<Box<dyn Draw>>) {
        for element in ui {
            element.draw();
        }
    }
    ```

    Здесь `ui` может содержать любые типы, реализующие `Draw`, и мы вызываем методы через динамическое связывание.

    Когда использовать:

    * Когда нужно хранить разные типы в одной коллекции;
    * Когда интерфейс известен, но конкретные типы будут определены во время выполнения;
    * Когда важна гибкость, а не максимальная производительность.

    Недостатки:

    * Runtime overhead (vtable lookup);
    * Нет инлайнинга методов.

    ---

</details>  

7. <details><summary>Как в Rust имитировать наследование?</summary>

    В Rust нет классического наследования, как в Java или C++, но его можно имитировать с помощью **композиции** и **трейтов**.
    Основная идея: вместо "является" (is-a) используем "имеет" (has-a) и "умеет" (can-do).

    ---

    ### 1. **Через трейты (интерфейсы поведения)**

    Если в ООП мы наследуем методы и переопределяем их, то в Rust это делается через трейты:

    ```rust
    trait Animal {
        fn speak(&self);
    }

    struct Dog;
    struct Cat;

    impl Animal for Dog {
        fn speak(&self) {
            println!("Woof!");
        }
    }

    impl Animal for Cat {
        fn speak(&self) {
            println!("Meow!");
        }
    }

    fn make_it_talk<T: Animal>(animal: T) {
        animal.speak();
    }

    fn main() {
        make_it_talk(Dog);
        make_it_talk(Cat);
    }
    ```

    Здесь `Dog` и `Cat` как бы "наследуют" поведение `Animal`.

    ---

    ### 2. **Композиция (вместо наследования свойств)**

    Вместо наследования полей из базового класса мы встраиваем их в структуру:

    ```rust
    struct Position {
        x: i32,
        y: i32,
    }

    struct Player {
        pos: Position,
        health: i32,
    }

    struct Enemy {
        pos: Position,
        damage: i32,
    }
    ```

    И если нужно общее поведение для `Position`, мы делаем трейт:

    ```rust
    trait Movable {
        fn move_to(&mut self, x: i32, y: i32);
    }

    impl Movable for Position {
        fn move_to(&mut self, x: i32, y: i32) {
            self.x = x;
            self.y = y;
        }
    }
    ```

    Теперь и `Player`, и `Enemy` могут иметь общий функционал через встраивание `Position`.

    ---

    ### 3. **Дефолтная реализация в трейтах (похоже на наследование с базовым методом)**

    ```rust
    trait Logger {
        fn log(&self, msg: &str) {
            println!("[LOG] {}", msg);
        }
    }

    struct Service;
    impl Logger for Service {}

    fn main() {
        let s = Service;
        s.log("Hello!"); // используем базовую реализацию
    }
    ```

    Можно переопределять методы в конкретных структурах — как в наследовании.

    ---

</details>

8. <details><summary>Что такое **default methods** в trait и зачем они нужны?</summary>  

    В Rust **default methods** — это методы в трейтах, которые имеют **реализацию по умолчанию**. Они позволяют определять поведение прямо внутри трейта, чтобы каждый тип, реализующий этот трейт, мог либо использовать готовый метод, либо переопределить его.

    Пример:

    ```rust
    trait Greet {
        fn name(&self) -> &str;

        fn greet(&self) { // метод по умолчанию
            println!("Hello, {}!", self.name());
        }
    }

    struct User { name: String }
    impl Greet for User {
        fn name(&self) -> &str { &self.name }
    }
    ```

    Здесь `User` автоматически получает метод `greet()` без дополнительного кода.

    Плюсы:

    * Уменьшение дублирования кода — общие методы пишутся один раз в трейте;
    * Возможность задать "разумное поведение" для всех реализаций;
    * Гибкость — каждая реализация может переопределить метод при необходимости.

    Используются, например, в стандартной библиотеке (`Iterator`, `Default`) для удобства и DRY-принципа.

    ---

</details>  

9. <details><summary>Чем **composition** в Rust предпочтительнее **inheritance**?</summary>  

    В Rust **composition** (композиция) — основной способ повторного использования кода. Вместо того, чтобы наследовать поведение от базового класса, вы создаёте новые структуры, **встраивая** другие структуры как поля, и реализуете нужные трейты.

    Проблемы **inheritance**:

    * Жёсткая иерархия затрудняет изменение кода;
    * Множественное наследование вызывает конфликты;
    * Базовые классы часто становятся "god objects", от которых зависит всё.

    Преимущества **composition** в Rust:

    * Гибкость — можно комбинировать разные структуры и трейты без сложных иерархий;
    * Меньше связанности — изменения в одном компоненте меньше влияют на другие;
    * Легче тестировать — каждый компонент можно проверять отдельно;
    * Безопасность — Rust исключает проблемы с "ромбовидным наследованием" и двусмысленным поведением.

    В итоге, композиция в Rust — это осознанный архитектурный выбор, который даёт чистый, модульный и безопасный код.

    ---

</details>  

10. <details><summary>Как связаны `Self`, `&self` и владение при методах в `impl`?</summary>  

    В Rust в методах `impl` есть три ключевых способа указать получателя:

    * `self` — **забирает владение** структурой. После вызова метод получает полный контроль над данными, а исходная переменная становится недействительной.
    * `&self` — принимает **ссылку без права изменения**. Позволяет читать данные, не забирая их. Эквивалентно `self: &Self`.
    * `&mut self` — принимает **изменяемую ссылку**, позволяя менять данные внутри объекта, но при этом владение остаётся у вызывающего кода.

    `Self` с большой буквы в сигнатуре метода или трейта — это имя текущего типа:

    ```rust
    impl MyStruct {
        fn new() -> Self { Self {} }
    }
    ```

    Здесь `Self` == `MyStruct`.

    Владение определяет, можно ли использовать значение после вызова метода:

    * Если метод принимает `self`, то объект перемещается;
    * Если `&self` или `&mut self`, то объект остаётся доступным.

    Это важно для безопасного управления ресурсами без сборщика мусора.

    ---

</details>  

---

**Принцыпы SOLID:**

1. <details><summary>S____</summary>  

    **S — Single Responsibility Principle (Принцип единственной ответственности)**
    Каждый класс или модуль должен иметь только одну зону ответственности.
    Идея в том, что класс выполняет одну задачу и делает это хорошо.

    **Почему это важно:**

    * Код легче поддерживать.
    * Изменения в одной части программы не ломают другую.
    * Проще тестировать отдельные модули.

    **Пример на Rust:**
    Плохо:

    ```rust
    struct Report {
        content: String,
    }

    impl Report {
        fn generate(&self) { /* генерация отчета */ }
        fn save_to_file(&self) { /* сохранение в файл */ }
    }
    ```

    Здесь `Report` и генерирует, и сохраняет данные — две разные ответственности.

    Хорошо:

    ```rust
    struct Report {
        content: String,
    }

    struct FileSaver;

    impl Report {
        fn generate(&self) { /* генерация отчета */ }
    }

    impl FileSaver {
        fn save(&self, report: &Report) { /* сохранение в файл */ }
    }
    ```

    Разделение обязанностей упрощает поддержку.

    ---

</details>  

2. <details><summary>_O___</summary>  

    **O — Open/Closed Principle (Принцип открытости/закрытости)**
    Программные сущности должны быть *открыты* для расширения, но *закрыты* для изменения.
    Это значит, что поведение можно добавлять, не переписывая существующий код.

    **Почему это важно:**

    * Меньше риска поломать уже работающий функционал.
    * Легче адаптировать код к новым требованиям.

    **Пример на Rust:**
    Вместо изменения старого кода, используем **трейты**:

    ```rust
    trait Shape {
        fn area(&self) -> f64;
    }

    struct Circle { radius: f64 }
    struct Square { side: f64 }

    impl Shape for Circle {
        fn area(&self) -> f64 { 3.14 * self.radius * self.radius }
    }

    impl Shape for Square {
        fn area(&self) -> f64 { self.side * self.side }
    }
    ```

    Теперь мы можем добавить `Triangle`, не трогая старые структуры.

    ---

</details>  

3. <details><summary>__L__</summary>  

    **L — Liskov Substitution Principle (Принцип подстановки Барбары Лисков)**
    Объекты подклассов должны быть взаимозаменяемы с объектами базового класса, не нарушая работу программы.

    **Почему это важно:**

    * Гарантирует предсказуемое поведение кода.
    * Упрощает повторное использование и тестирование.

    ---

</details>  

4. <details><summary>___I_</summary>  

    **I — Interface Segregation Principle (Принцип разделения интерфейсов)**
    Клиенты не должны зависеть от интерфейсов, которые они не используют.
    Лучше иметь несколько маленьких интерфейсов (трейтов), чем один огромный.

    **Почему это важно:**

    * Исключает лишние зависимости.
    * Повышает модульность кода.

    **Пример на Rust:**
    Плохо:

    ```rust
    trait Worker {
        fn work(&self);
        fn eat(&self);
    }
    ```

    Не всем работникам нужен метод `eat`.

    Хорошо:

    ```rust
    trait Work {
        fn work(&self);
    }

    trait Eat {
        fn eat(&self);
    }
    ```

    Теперь объекты реализуют только нужные методы.

    ---

</details>  

5. <details><summary>____D</summary>  

    **D — Dependency Inversion Principle (Принцип инверсии зависимостей)**
    Модули верхнего уровня не должны зависеть от модулей нижнего уровня. И те, и другие должны зависеть от абстракции. Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.

    **Почему это важно:**

    * Позволяет легко менять реализацию.
    * Упрощает тестирование.

    **Пример на Rust:**
    Плохо:

    ```rust
    struct MySQLDatabase;
    impl MySQLDatabase { fn query(&self) {} }

    struct App {
        db: MySQLDatabase,
    }
    ```

    `App` зависит от конкретной БД.

    Хорошо:

    ```rust
    trait Database {
        fn query(&self);
    }

    struct MySQLDatabase;
    impl Database for MySQLDatabase {
        fn query(&self) {}
    }

    struct App<T: Database> {
        db: T,
    }
    ```

    Теперь можно подменить БД, не трогая `App`.

    ---

</details>  

---

**Практические задания:**

* <details><summary>Реализовать интерфейс для геометрических фигур (`Circle`, `Rectangle`) с методом `area()`.</summary>  

    ```rust
    trait Shape {
        fn area(&self) -> f64;
    }

    struct Circle {
        radius: f64,
    }

    struct Rectangle {
        width: f64,
        height: f64,
    }

    impl Shape for Circle {
        fn area(&self) -> f64 {
            std::f64::consts::PI * self.radius.powi(2)
        }
    }

    impl Shape for Rectangle {
        fn area(&self) -> f64 {
            self.width * self.height
        }
    }

    fn main() {
        let c = Circle { radius: 3.0 };
        let r = Rectangle { width: 4.0, height: 5.0 };

        println!("Circle area: {}", c.area());
        println!("Rectangle area: {}", r.area());
    }
    ```

    Здесь `Shape` — трейт, а `Circle` и `Rectangle` реализуют его по-своему. Это даёт единый интерфейс для работы с разными фигурами.

    ---

</details>  

* <details><summary>Сделать систему плагинов через `Box<dyn Trait>`.</summary>  

    ```rust
    trait Plugin {
        fn name(&self) -> &str;
        fn run(&self);
    }

    struct HelloPlugin;
    impl Plugin for HelloPlugin {
        fn name(&self) -> &str { "HelloPlugin" }
        fn run(&self) {
            println!("Hello from plugin!");
        }
    }

    struct GoodbyePlugin;
    impl Plugin for GoodbyePlugin {
        fn name(&self) -> &str { "GoodbyePlugin" }
        fn run(&self) {
            println!("Goodbye from plugin!");
        }
    }

    fn main() {
        let plugins: Vec<Box<dyn Plugin>> = vec![
            Box::new(HelloPlugin),
            Box::new(GoodbyePlugin),
        ];

        for plugin in plugins.iter() {
            println!("Running {}", plugin.name());
            plugin.run();
        }
    }
    ```

    Используем `Box<dyn Plugin>` для хранения объектов разных типов в одном векторе. Это пример **динамического полиморфизма** в Rust, когда тип выбирается во время выполнения.

    ---

</details>  

---

## 🧩 Функциональное программирование в Rust

**Теоретические вопросы:**

1. <details><summary>Что такое **immutability** в Rust и как оно связано с безопасностью?</summary>  

    В контексте функционального программирования (ФП) immutability в Rust — это принцип, по которому данные после создания не изменяются. Rust реализует это через неизменяемые переменные по умолчанию (let вместо let mut). Это свойство прямо вытекает из идей ФП, где программы описывают вычисления как преобразования неизменяемых значений, а не как последовательность изменений состояния.

    В Rust неизменяемость тесно связана с безопасностью:

    Отсутствие состояния гонки (data races) — если переменная неизменяема, несколько потоков могут читать её одновременно без синхронизации. Это делает параллельное программирование безопаснее.

    Предсказуемость кода — функции, работающие только с неизменяемыми данными, становятся "чистыми" (pure functions): они не имеют побочных эффектов и всегда дают одинаковый результат на одинаковых входных данных. Это облегчает тестирование, отладку и формальное доказательство корректности.

    Явное разрешение на изменение — если нужно изменить данные, требуется mut. Такой явный сигнал уменьшает вероятность случайных модификаций.

    В ФП неизменяемость — фундамент: она позволяет строить программы из комбинаций функций без опасений, что промежуточные шаги сломают состояние. Rust не чисто функциональный, но он заимствует этот подход, чтобы совмещать эффективность низкоуровневого кода с безопасностью и предсказуемостью ФП.

    ---

</details>  

2. <details><summary>Чем функции в Rust ближе к ФП, чем в ООП-языках?</summary>  

    Функции в Rust ближе к функциональному программированию (ФП), потому что:

    * Они могут быть **чистыми** (pure functions), не имеющими побочных эффектов и всегда возвращающими одинаковый результат при одинаковых входных данных.
    * Поддерживаются **замыкания** (closures), позволяющие передавать функции как значения.
    * Rust поддерживает **функции высшего порядка** — те, которые принимают или возвращают функции.
    * Широко используются **итераторы** с методами `map`, `filter`, `fold` и другими цепочками, что позволяет писать декларативный, а не императивный код.
    * Неизменяемость по умолчанию (`immutability`) делает подход к работе с данными ближе к ФП, где состояние не меняется.
    В отличие от ООП, где логика часто привязана к объектам, в Rust функции могут существовать сами по себе, а полиморфизм реализуется через трейты, а не наследование.

    ---

</details>  

3. <details><summary>Что такое **замыкания** в Rust? Разница между `Fn`, `FnMut` и `FnOnce`.</summary>  

    Замыкания (**closures**) — это анонимные функции, которые могут захватывать переменные из окружающей области видимости. Они часто используются как аргументы функций высшего порядка или при работе с итераторами.
    Разница между типажами:

    * **`Fn`** — замыкание, которое захватывает переменные только по **ссылке** (`&T`), не изменяя их. Может вызываться многократно.
    * **`FnMut`** — захватывает переменные **по изменяемой ссылке** (`&mut T`). Может изменять захваченные данные и вызываться несколько раз.
    * **`FnOnce`** — захватывает переменные **по значению** (владеет ими) и может быть вызвано только **один раз**, потому что после первого вызова данные будут перемещены.
    Компилятор сам определяет минимально необходимый трейт, исходя из того, как замыкание использует внешние переменные.

    ---

</details>  

4. <details><summary>Как работают функции высшего порядка в Rust? Примеры.</summary>  

    Функции высшего порядка (Higher-Order Functions, HOF) — это функции, которые принимают другие функции в качестве аргументов или возвращают функции как результат. Rust активно использует их для обработки данных декларативно.
    Пример:

    ```rust
    fn apply_to_vec<F>(v: Vec<i32>, f: F) -> Vec<i32>
    where
        F: Fn(i32) -> i32,
    {
        v.into_iter().map(f).collect()
    }

    fn main() {
        let nums = vec![1, 2, 3];
        let doubled = apply_to_vec(nums, |x| x * 2);
        println!("{:?}", doubled); // [2, 4, 6]
    }
    ```

    Это позволяет писать компактный, модульный код. Вместо циклов `for` можно описывать операции над данными в виде цепочек вызовов методов.

    ---

</details>  

5. <details><summary>Что такое `Option` и `Result` как монады? (без глубокого теоретического формализма).</summary>  

    `Option<T>` — контейнер, который либо содержит значение (`Some(T)`), либо пуст (`None`). Используется для случаев, когда результат может отсутствовать.
    `Result<T, E>` — контейнер для значений, которые могут быть успешными (`Ok(T)`) или ошибочными (`Err(E)`).
    Они ведут себя как монады в том смысле, что предоставляют методы (`map`, `and_then`), позволяющие **цепочечно обрабатывать значения без явных проверок `match` на каждом шаге**. Вместо кучи `if`/`else` можно писать:

    ```rust
    let res = Some(2).map(|x| x * 3).and_then(|x| if x > 5 { Some(x) } else { None });
    ```

    Это делает код чище, избавляет от явных проверок на пустоту/ошибку и уменьшает вероятность пропустить обработку.

    ---

</details>  

6. <details><summary>Что такое `map`, `and_then` и `unwrap_or`? Примеры использования.</summary>  

    * **`map`** — применяет функцию к значению внутри `Option` или `Result`, если оно есть.

    ```rust
    Some(2).map(|x| x * 2); // Some(4)
    ```

    * **`and_then`** — как `map`, но функция должна возвращать ещё один `Option`/`Result`, что удобно для цепочек.

    ```rust
    Some(2).and_then(|x| if x > 1 { Some(x * 2) } else { None });
    ```

    * **`unwrap_or`** — возвращает значение внутри контейнера или подставляет дефолтное, если контейнер пуст/с ошибкой.

    ```rust
    None.unwrap_or(10); // 10
    ```

    Эти методы позволяют работать с контейнерами декларативно, без кучи проверок `match`.

    ---

</details>  

7. <details><summary>Как использовать **итераторы** и методы `map`, `filter`, `fold`?</summary>  

    Итераторы в Rust позволяют обрабатывать коллекции в декларативном стиле.

    * **`map`** — преобразует каждый элемент.

    ```rust
    vec![1, 2, 3].into_iter().map(|x| x * 2).collect::<Vec<_>>(); // [2, 4, 6]
    ```

    * **`filter`** — отбирает элементы по условию.

    ```rust
    vec![1, 2, 3].into_iter().filter(|&x| x > 1).collect::<Vec<_>>(); // [2, 3]
    ```

    * **`fold`** — аккумулирует значения.

    ```rust
    vec![1, 2, 3].into_iter().fold(0, |acc, x| acc + x); // 6
    ```

    Можно цеплять методы:

    ```rust
    let sum = vec![1, 2, 3].into_iter()
        .filter(|&x| x > 1)
        .map(|x| x * 2)
        .fold(0, |acc, x| acc + x);
    ```

    Это делает код выразительным и избавляет от явных циклов.

    ---

</details>  

8. <details><summary>Чем `match` в Rust отличается от `switch` в C/C++?</summary>  

    `match` в Rust — это мощный инструмент **pattern matching**, а не просто проверка на равенство, как `switch` в C/C++. Он позволяет:

    * Сопоставлять не только константы, но и диапазоны, кортежи, структуры, `enum` и даже сложные условия через `if guard`.
    * Гарантировать, что все варианты будут обработаны (exhaustive checking).
    * Извлекать значения прямо в блоке сопоставления.
    Пример:

    ```rust
    match num {
        0 => println!("Zero"),
        1..=10 => println!("Small number"),
        _ => println!("Other"),
    }
    ```

    В C/C++ `switch` проверяет только целочисленные значения и требует `break`, чтобы избежать fallthrough. В Rust этого нет — каждый блок `match` чётко отделён.

    ---

</details>  

9. <details><summary>Как работает `if let` и `while let`?</summary>  

    `if let` и `while let` — это синтаксический сахар для упрощённого pattern matching, когда нас интересует только один конкретный вариант.
    Пример `if let`:

    ```rust
    if let Some(x) = maybe_num {
        println!("Got {}", x);
    }
    ```

    Это эквивалентно:

    ```rust
    match maybe_num {
        Some(x) => println!("Got {}", x),
        _ => {}
    }
    ```

    `while let` аналогичен, но работает в цикле, пока значение соответствует шаблону:

    ```rust
    while let Some(x) = stack.pop() {
        println!("{}", x);
    }
    ```

    Это удобно для обработки потоков данных или контейнеров, пока есть элементы.

    ---

</details>  

10. <details><summary>Что такое pattern matching по структурам и enum'ам?</summary>  

    Pattern matching в Rust позволяет сопоставлять значения с шаблонами и извлекать данные из сложных структур.
    Пример с `struct`:

    ```rust
    struct Point { x: i32, y: i32 }
    let p = Point { x: 1, y: 2 };
    match p {
        Point { x, y: 0 } => println!("On X axis at {}", x),
        Point { x: 0, y } => println!("On Y axis at {}", y),
        Point { x, y } => println!("At ({}, {})", x, y),
    }
    ```

    Пример с `enum`:

    ```rust
    enum Shape {
        Circle(f64),
        Rectangle(f64, f64),
    }
    let s = Shape::Circle(3.0);
    match s {
        Shape::Circle(r) => println!("Radius {}", r),
        Shape::Rectangle(w, h) => println!("{} x {}", w, h),
    }
    ```

    Это позволяет работать с разными типами данных безопасно и без ручных проверок типов.

    ---

</details>  

**Практические задания:**

* <details><summary>Используя `map` и `filter`, посчитать сумму всех чётных чисел в векторе.</summary>  

    ```rust
    fn main() {
        let numbers = vec![1, 2, 3, 4, 5, 6];
        let sum_even: i32 = numbers
            .into_iter()
            .filter(|n| n % 2 == 0) // оставляем только чётные
            .sum(); // суммируем
        println!("Сумма чётных: {}", sum_even);
    }
    ```

    ---

</details>  

* <details><summary>Написать функцию, которая принимает замыкание и применяет его к каждому элементу списка.</summary>  

    ```rust
    fn apply_to_each<T, F>(list: Vec<T>, mut func: F)
    where
        F: FnMut(&T),
    {
        for item in &list {
            func(item);
        }
    }

    fn main() {
        let numbers = vec![1, 2, 3];
        apply_to_each(numbers, |n| println!("Элемент: {}", n));
    }
    ```

    ---

</details>  

* <details><summary>Сделать обработку списка `Option<String>` так, чтобы вернуть все строки в верхнем регистре без `None`.</summary>  

    ```rust
    fn main() {
        let data = vec![
            Some("hello".to_string()),
            None,
            Some("world".to_string()),
        ];

        let uppercased: Vec<String> = data
            .into_iter()
            .filter_map(|opt| opt) // убираем None
            .map(|s| s.to_uppercase()) // в верхний регистр
            .collect();

        println!("{:?}", uppercased);
    }
    ```

    ---

</details>  



